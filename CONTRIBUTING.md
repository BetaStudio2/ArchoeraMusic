# ArchoeraMusic 开发规范（贡献者与协作者必读）

> **温馨提醒**：开发者可能呆但不傻，请在完整阅读完贡献条款，确认您可以接受后，再进行 fork、PR 等操作。

> **太长不看版**：
> 1. 向本仓库提交代码即表示接受 [README「贡献者授权」](README.md#22-贡献者授权contributor-license-grant)（AGPL-3.0-or-later 不可撤销再许可授权）；
> 2. 提交前本地跑通 `flutter analyze` + `flutter test`（改动原生模块时另跑对应构建/测试）；
> 3. Commit 遵循 `type(scope): 中文描述`（Conventional Commits，见 §7）；
> 4. 新增第三方依赖必须 AGPL 兼容，并在对应模块 `THIRD-PARTY-LICENSES.md` 逐项登记（§9）；
> 5. 架构戒律（零 TCP 端口 / 纯 Dart 平台协议层 / FFI 直连 / 事件总线）不可绕过（§4）；
> 6. **红线零容忍（§2.1–2.5）**：广告 / 付费解锁 / 内网穿透（frp 等）/ 遥测上报 / 非 FFI 大运行时（Node、Python 等）；踩一捧一、不公平比对（含 FFmpeg vs EraAudio）、谩骂引战；代码零个人数据、禁强制联网验证与离线功能限制、用户数据仅存本地并标注位置、禁音乐平台外账户登录、禁反向用户定位与非法数据获取；禁平台间歧视与故意兼容冲突（仅硬性限制可协商，其余一律不合并）；禁代码舞弊、系统强行注入、编译依赖链攻击、引入/调用私人仓库等非公开模块与接口——一律不得引入（另：高度不建议第三方「预制菜」整包引入，§2.2/§9）；
> 7. **先本地测试再提交**：`analyze` + `test` 全绿才能 push；AI 生成代码必须经人工逐行审核并附测试证据，否则 PR 直接拒绝。
> 8. **权限公平（§2.6）**：维护者不得借仓库权限威胁、设隐性合并门槛或无故拒绝合并；贡献不得附带隐性要求 / 强制许可要求；谩骂等违规 PR / Issue 无解释直接关闭。
> 9. **安全漏洞私密上报**（[SECURITY.md](SECURITY.md)）：禁止公开 Issue / PR / Discussion 讨论漏洞细节；正常缺陷（UI Bug / 调用 bug / 通讯异常 / 崩溃等）照常走公开 Issue / PR。
>
> 本规范对所有协作者、贡献者生效；与 README 许可条款冲突时以 README / LICENSE 为准。

---

## 目录

1. [许可与贡献者授权（先读这个）](#1-许可与贡献者授权先读这个)
2. [红线（零容忍）](#2-红线零容忍)
3. [环境与构建基线](#3-环境与构建基线)
4. [架构戒律（硬性规则）](#4-架构戒律硬性规则)
5. [代码规范（分语言）](#5-代码规范分语言)
6. [测试与验收门槛](#6-测试与验收门槛)
7. [提交规范（Commit / PR）](#7-提交规范commit--pr)
8. [文档规范](#8-文档规范)
9. [第三方依赖与许可合规](#9-第三方依赖与许可合规)
10. [安全规范](#10-安全规范)
11. [仓库卫生](#11-仓库卫生)
12. [Issue 与缺陷报告](#12-issue-与缺陷报告)
13. [维护者职责（版本 / 发布 / 决策记录）](#13-维护者职责版本--发布--决策记录)

---

## 1. 许可与贡献者授权（先读这个）

- 本项目整体以 **AGPL-3.0-or-later** 发布（仓库根 `LICENSE`）。任何 PR / patch / 直接推送均视为
  已接受 [README §2.2 贡献者授权](README.md#22-贡献者授权contributor-license-grant)：
  授权版权持有者 BetaStudio2 将贡献代码连同项目整体以「AGPL-3.0 及任何更高版本」再许可、分发与修改。
  **PR 合入前需要通过签名或显式确认接受该条款**。
- 贡献者署名权保留（git author / changelog），署名请使用真实可追溯的身份标识。
- 对许可证边界有疑问（尤其是第三方代码引入、闭源集成）：**先开 Issue 咨询，再动手**。

## 2. 红线（零容忍）

以下为本项目所有者设定的**不可协商红线**，适用于所有协作者、贡献者与维护者（含仓库写权限持有者）。
命中任意一条的提交 / PR / Issue 一律**无解释直接关闭**，不进入其余内容评审；毁灭性 / 破坏性提交
另按 §2.3 封禁处理；对红线本身的增删视同架构级决策，须经所有者书面批准（§13）。

### 2.1 内容与商业模式红线（产品形态）

- **禁止广告**：任何形式的广告 SDK、开屏 / 横幅 / 信息流广告、推广位、赞助内容注入均不得引入。
- **禁止任何「付费才能解锁」模块**：会员墙、内购、订阅、限时限次解锁、功能付费等一律不得出现在
  播放器内——内容与功能的完整体验不得与付费挂钩；本项目不提供闭源商业授权（README 使用声明）。
- **禁止内网穿透 / 隧道类模块**：frp、ngrok 及同类反向隧道 / 内网上线工具一律禁止，包括为其预留的
  接口、配置项或「可选依赖」（与既有的本地 Subsonic 服务端无关，后者不涉及公网穿透）。
- **禁止遥测 / 埋点 / 数据上报**：应用内统计、用户行为追踪、云端崩溃上报 SaaS 均不得引入；
  诊断只允许本地日志（`logs/`），且不含敏感信息（§10）。
- **禁止捆绑与静默行为**：不捆绑与核心功能无关的推广内容、不做静默下载、不起后台常驻服务。

### 2.2 技术红线（架构形态）

- **禁止非 FFI 大运行时**：Node、Python、JVM 等无法与 Dart FFI 直连的运行时，一律不得作为业务载体
  引入（Node 侧车已于 2026-09-06 整体废弃，不可逆）；子进程仅限 `archoera-vault`
  （C# NativeAOT，安全边界，stdio）。新增语言栈必须先走 §13 决策修订流程，未批准不得开工。
- **禁止平台间歧视与故意兼容破坏**：不得以「跨平台」为理由故意不兼容、故意冲突，甚至故意引发系统问题
  （如针对某端故意降级体验、故意触发他端缺陷、破坏文件关联 / 污染系统配置等）；不得贬低或歧视任何
  平台及其用户。**合并口径**：仅「平台硬性限制导致的部分功能不可用」可协商合并（PR 须附限制依据、
  文档说明与降级方式）；**其他任何形式的跨平台兼容冲突一律不合并**。
- **禁止强制提权与高危调用**：应用与模块不得强制要求管理员 / root 身份启动（sudo / Administrator），
  默认以普通用户权限运行；程序内禁止高危调用与系统级命令（如 `sudo rm -rf`、批量无差别删除、
  注册表 / 系统配置写入、杀进程等）；文件操作仅限用户显式选择的目录并带确认流程，
  清理操作限定在应用自身数据目录内。
- **禁止代码「舞弊」**：不得以任何形式造假或规避检测——基准作弊（识别基准程序、硬编码结果、
  针对测试语料特判）、测试舞弊（检测测试环境后改变行为、伪造测试输出、空断言凑数）、
  功能造假（假进度 / 假完成 / 假统计）等。
- **禁止系统（强行）注入**：不得引入对其他进程或系统组件的任何强行注入与挂钩
  （DLL / SO 注入、`LD_PRELOAD` 劫持、hook 第三方进程、模拟输入劫持等）；本项目进程只管理自身。
- **禁止编译内依赖链攻击**：不得借构建与依赖链实施任何攻击或越权行为——构建脚本暗装后门、
  依赖投毒 / typosquatting、postinstall 越权执行、编译期联网拉取未登记产物、劫持工具链等；
  构建期行为必须可审计、可离线复现。
- **禁止引入 / 调用非公开模块与接口**：不得以任何形式引入或调用私人仓库等非公开模块与接口——
  私有 git 依赖、私有 pub / crate / go / npm 源、闭源 SDK、未公开的内部接口约定等；
  一切依赖必须公开可获取、可审计、可复现构建（§9）。运行时对第三方网络 API 的调用不属此列，
  但仍受 §2.1 / §2.5 红线约束。
- **高度不建议基于第三方「预制菜」制作与编译引入**：不在他人整套现成实现 / 预编译产物 /
  不明来源构建结果之上做开发或编译（防上游投毒、许可污染与不可审计黑盒）。库级依赖按 §9
  登记引入；确需 vendored / 参考移植的，必须来源可溯、许可合规、逐项登记
  `THIRD-PARTY-LICENSES.md` 并在 PR 说明理由（现状合规案例：AMR OpenCORE vendored 源、
  AMLL 以 Dart 重实现）。

### 2.3 流程红线（提交纪律）

- **不本地测试不提交**：push 前必须本地跑通 `cd app && flutter analyze`（0 issue）+
  `flutter test`（全绿）；触碰原生模块时另跑对应构建 / 测试（§6）。CI 红了按自己的缺陷修复，
  禁止「先合了再说」「下个 PR 再修」。
- **AI 生成代码必须人工审核 + 测试**：AI 辅助生成的代码视为提交者本人编写，**质量与许可责任完全归属
  提交者**；PR 必须声明 AI 参与程度（轻度补全 / 大段生成）；**未经人工逐行审核、或未附本地测试证据的
  AI 代码 PR 一律直接拒绝**——不接受「AI 写的我不太懂」类说明。
- **代码纯净**：PR 只含与目标相关的改动，不夹带注释掉的死代码、调试 print / 临时 hack、
  无关文件的重排与格式化噪音、IDE 配置噪音、无关依赖升级。
- **禁止附带隐性要求 / 强制许可证要求**：贡献不得附带任何额外条件（署名权变更、要求合入无关改动、
  利益交换等）；项目许可为 AGPL-3.0-or-later（§1），**任何以「合并 PR」为条件的许可要求一律拒绝**，
  此类 PR / Issue 直接无解释关闭；对许可本身的异议走 Issue 理性讨论（§1）。
- **禁止毁灭性 / 破坏性 PR**：任何以损害项目为目的的破坏性提交（大面积无理由删除、故意破坏构建、
  植入危险逻辑、污染提交历史等），无论动机（包括但不限于私人仇恨），一律无解释关闭并视情况封禁。

### 2.4 对外言论与社区行为红线

- **禁止「踩一捧一」**：不得以贬低任何其他项目（FFmpeg、SPlayer-Next、KuGouMusicApi、
  NeteaseCloudMusicApi 等上游 / 参考 / 同类项目）的方式抬高本项目；宣传与对比必须基于事实与可复现数据。
- **禁止不公平比对**：所有对外比较必须方法对等、口径透明、优劣并陈、标注局限。**特别约束
  FFmpeg vs EraAudio**：比较口径以仓库内基准文档为准（`docs/benchmark-industry-2026-09-05.md` 等），
  其「实验性参考、非发布承诺」定位与**已知短板声明**（如 flac 直解仍落后 FFmpeg 约 20×、
  tta/dts 内存峰值偏高等）必须一并呈现，**不得选择性引用有利数据**；FFmpeg 是本项目默认主引擎与
  最大依赖，对其保持尊重与致谢（见 README 致谢节）。
- **禁止谩骂**：任何 Issue / PR / 代码批注（review comments / 代码注释 / 文档措辞）/ 讨论中，
  对任何项目、作者、贡献者的侮辱、人身攻击、嘲讽一律禁止；对其他项目有意见走其官方渠道
  （Issue / 邮件列表）理性反馈，不借本仓库发泄。
- **禁止拉仇恨 / 引战**：不得发布、转发或暗示任何煽动对立的内容（播放器阵营对立、许可证立场攻击、
  群体地图炮等）。
- **高度不建议跨项目提及与传播**：不在其他项目的评论区 / Issue / 社区等场合提及或传播本项目与该项目的
  对比（无论褒贬），也不以其他任何形式跨项目引流；把对比内容带到他人社区极易引发争端、
  也打扰对方维护者——想推荐本项目，请在自己的渠道（博客 / 视频 / 社群）进行。

> 执行口径：以上前四项零容忍（命中即无解释直接关闭 / 要求撤回），末项「高度不建议」为强烈劝阻——
> 不自动关 PR，但维护者会公开要求修正；反复违反按零容忍处理。
> 所有谩骂等违反本规范的 PR / Issue 一律无解释直接关闭。

### 2.5 隐私与离线红线（数据与账户）

- **代码零隐私 / 零个人数据**：所有开发者必须保证提交的代码内不包含任何隐私与个人数据——
  真实账号 / cookie / token / 密钥、个人路径与设备名、真实用户样本与截图、含个人信息的测试夹具
  一律禁止；测试凭据一律使用明确标注的假数据（`test` / 占位符）。
- **禁止强制联网验证**：不得引入启动、解锁或功能可用性上的强制在线校验（激活码联网核验、
  强制登录才能进入应用、远程开关功能、云端租期 / 授权到期等）；应用断网必须可正常启动并使用。
- **离线全功能**：非联网状态下禁止一切形式的功能限制——本地曲库、扫描、播放、队列、下载管理、
  设置等核心能力在无网络时完整可用（刮削 / 歌词在线源离线时允许失败回退，不得报错阻断）。
  唯一例外是**在线音源本身**（网易云 / 酷狗 / QQ 音乐 / 流媒体服务器对网络的硬依赖，属非可抗因素），
  且不得借在线能力反向限制离线功能。
- **数据存储本地化与位置标注**：新增任何数据存储模块（数据库 / 缓存 / 日志 / 配置 / 临时文件）时，
  必须在 PR 与对应文档中**注明存储位置**（应用数据目录下的具体路径与访问方）；用户数据
  **必须存储在用户本地**，禁止以任何形式上传至其他方——「匿名统计」「错误报告」「云同步」等包装
  同样视为上传，零容忍。
- **禁止反向用户定位与非法数据获取**：不得引入任何形式的**反向用户定位**能力——设备指纹、
  追踪标识符（广告 ID / 持久化设备 ID / 跨会话跨启动追踪）、IP 级地理定位、隐蔽上报坐标、
  跨应用关联识别等；不得具备任何**对用户数据的非法 / 越权获取**能力——未获用户明确同意、
  超出功能声明范围的数据采集（越权读文件 / 凭据 / 通讯录、绕过系统权限、隐蔽采集、
  从日志缓存提取他人数据等）一律禁止。用户显式授权的能力（如天气功能的系统定位）必须走
  平台能力外观层（`docs/platform-capability-facade.md`）：可关闭、仅用于该功能本身、不做二次利用。
- **禁止音乐平台之外的账户登录**：账户体系仅限音乐内容提供方——在线音乐平台
  （网易云 / 酷狗 / QQ 音乐）与用户自托管音乐服务端（Subsonic / Jellyfin，凭据仅在本机与该服务端间
  使用）；**尤其禁止**引入提供用户个人数据存储服务的账户登录：要求注册本项目账号、第三方云账户
  绑定 / 同步、厂商 ID 登录等一律红线。

### 2.6 权限与维护红线（协作者 / 维护者）

以下约束**对等适用**于拥有仓库写权限的协作者与维护者；违反视同红线，由所有者处理（直至撤销权限）：

- **禁止借权限威胁**：不得以合并权限、关闭 / 锁定 Issue、打标签、CI 状态等任何仓库能力，
  对他人的提交、Issue 或个人进行威胁、施压或报复。
- **禁止隐性合并要求**：合并标准以本文为准，不得在成文规则之外设置任何「隐性合并要求」
  （未公开的口头门槛、站队要求、私人交换条件等）。
- **禁止故意不合并**：满足本规范全部门槛（红线、戒律、测试、文档、许可）的 PR 不得无故搁置或
  故意拒绝；不予合并必须给出可追溯的书面理由（命中红线者除外——红线按「无解释直接关闭」执行，
  见 §2 开头）。
- **优质代码一视同仁**：接受包括 AI 在内的一切优质代码产出（仍须满足 §2.3 的人工审核与测试要求）；
  合并与否仅取决于代码质量与本规范的符合度，与贡献者身份无关。

### 2.7 处理梯度与申诉

处理由项目所有者执行（或其书面授权的维护者）；对拥有写权限者的违规同样适用（§2.6）：

| 档位 | 触发 | 处理 |
|---|---|---|
| 关闭 | 违反红线 / 贡献规则 | 无解释直接关闭（§2 开头口径） |
| 警告 → 临时封禁 | 同类违规反复（≥ 2 次） | 先警告（公开或私信），再犯临时封禁（≥ 30 天，期限由所有者定） |
| 永久封禁 | 毁灭性 / 破坏性提交、代码舞弊、注入 / 供应链投毒、恶意程序、窃取用户数据类 | 永久封禁，不适用常规申诉 |

- **申诉**：警告与临时封禁允许**一次**书面申诉（走私密渠道：GitHub 私信或所有者公示的联系方式，
  注明理由与证据）；永久封禁仅在「事实认定错误」时可申诉一次。申诉由所有者复核，结论为最终结论。
- 梯度处理不免除 §2 的「无解释直接关闭」——关闭本身不解释；封禁与申诉沟通在私密渠道进行。

---

## 3. 环境与构建基线

前置环境与逐模块构建命令以两处为准（本节不复述细节）：

- **用户自编译手册**：[docs/user-build-from-source.md](docs/user-build-from-source.md)（太长不看版在文首）
- **CI 真实门禁**：[.github/workflows/build-all.yml](.github/workflows/build-all.yml)（三端并行构建 + `pub get / analyze / test` + Zig `build test` + vault 端到端）

速查（Linux 开发日常）：

```bash
bash app/core/build-linux.sh        # 全部原生模块（幂等；内核改动后必须重跑，见下）
cd app && flutter pub get           # 依赖变更后
cd app && flutter analyze           # 静态检查，必须 0 issue
cd app && flutter test              # 单元 + Widget 测试
cd app && flutter run -d linux      # 本地调试
```

注意点：

- **构建顺序敏感**：改了 Zig 内核（`app/core/audio-engine/kernel/`）必须先
  `zig build -Doptimize=ReleaseFast` 再 CMake 构建 audio-engine，否则链接陈旧产物。
- Windows 用 `app/core/build_windows.bat`（vcpkg + MSVC 一站式）；macOS 用
  `bash app/core/build-macos.sh osx-arm64`。
- 三端行为一致性是验收标准：涉及 FFI / 平台壳 / 路径解析的改动应尽量**三端验证**（理想三端全验），
  未覆盖的端必须在 PR 说明原因与风险（戒律 #12）。

## 4. 架构戒律（硬性规则）

以下规则源自 [docs/architecture.md](docs/architecture.md) 的已确认决策记录，**违反即打回**；
确需变更走「决策修订」流程（§13）：

| # | 戒律 | 说明 |
|---|---|---|
| 1 | **桌面端零 TCP 端口** | 不引入任何本地监听端口 / loopback 服务；进程间通信只用 FFI 与受控子进程（当前仅 `archoera-vault`，stdio） |
| 2 | **纯 Dart 平台协议层** | 网易云 / 酷狗 / QQ 音乐等平台 API 签名与请求逻辑全部纯 Dart（`app/lib/apis/`、`app/lib/services/<platform>/`），不引入 Node 侧车、不起子进程跑业务 |
| 3 | **重型处理走 FFI 直连原生** | 音频引擎（C）、扫描（C#）、刮削（C++）、下载（Rust）、Subsonic（Go）均为进程内 FFI；新增原生能力优先挂在既有模块，不开新语言栈 |
| 4 | **UI 不直连传输层** | 引擎 / 扫描 / 下载事件统一经 Dart 事件总线（`stores/event_bus`）转译为 `player:*` 等事件；UI 层只消费总线与 Riverpod 状态 |
| 5 | **状态管理 Riverpod，路由 go_router** | 不混入其他状态方案；跨页状态进 `stores/`，页面局部状态留在页面 |
| 6 | **下沉需证据驱动** | Dart → 原生的性能下沉必须 `profile → 命中阈值 → 只下沉该模块 → 回归对比`，不主动重写、不预防性优化（决策 #16） |
| 7 | **存储隔离** | 媒体库 `library.db` 与用户库 `user.db` 物理隔离，均 WAL + busy_timeout；新增表/迁移须写清归属与访问方（架构 §8）；配置走 `config/settings.json` 原子写 + 迁移，不做裸读写 |
| 8 | **隐藏即静默** | 高频推送（FFT 频谱 / position / 桌面歌词）在 UI 不可见时必须停止轮询或推送（架构 §5.5） |
| 9 | **Flutter 零解析负担** | 歌词等复杂数据的解析 / 标准化在 Dart 业务层完成，UI 只消费标准化行数组（架构 §10.2） |
| 10 | **FFI 契约零回归** | `engine_bindings` / `scanner-ffi` / downloader FFI 等既有符号与行为是契约；变更须保持向后兼容（或同步更新两侧 + 文档 + 测试），行为零回归 |
| 11 | **禁大子进程 / 非 FFI 运行时（去侧车化不可逆）** | Node、Python、JVM 等无法与 Dart FFI 直连的运行时一律不引入（Node 侧车已于 2026-09-06 整体废弃，见 §2.2）；子进程仅限 `archoera-vault`（安全边界，stdio）；新语言栈先走 §13 决策修订 |
| 12 | **多端一等公民** | Linux / Windows / macOS 三端能力对齐；平台差异收敛到平台能力外观（`docs/platform-capability-facade.md`）+ 每平台 adapter，能力缺失静默降级；**禁止「只在一端悄悄可用」**；平台相关改动（路径 / 编码 / 窗口 / 系统集成）三端审视（Windows 侧注意 ANSI 代码页 / 窄路径等编码边界，参考 e4840c2f），未验证的端在 PR 显式说明；合并口径：仅平台硬性限制导致的局部功能缺失可协商合入（附依据），其余跨平台兼容冲突一律不合并（红线 §2.2） |

## 5. 代码规范（分语言）

### 5.1 Dart / Flutter（`app/lib/`、`app/test/`）

- **Lint**：以 `app/analysis_options.yaml`（`flutter_lints`）为准，`flutter analyze` 必须零告警；
  不得用 `// ignore:` 压警告除非附注释说明原因。
- **目录归属**（现行结构，新代码对号入座）：

  | 目录 | 放什么 |
  |---|---|
  | `lib/apis/` | 纯 Dart 平台 API 协议实现（netease / kugou / qqmusic / lyric） |
  | `lib/app/` | 应用壳：bootstrap / router / shell / theme_provider |
  | `lib/pages/` `lib/widgets/` `lib/settings/` `lib/theme/` | 页面、组件、设置弹窗、主题 |
  | `lib/services/` | 业务层：playback / scanner / scraper / downloader / subsonic / lyrics / streaming 等 |
  | `lib/stores/` | Riverpod 状态：app_prefs / playback_session / event_bus |
  | `lib/eta/` | 自建图标字体引用（见 `lib/eta/README.md`） |

- **文案（i18n）**：所有面向用户的文案必须走 gen_l10n 管道（`l10n.yaml`，模板 `app_zh_CN.arb`），
  新增 key 同步补 `app_en_US.arb`；禁止硬编码中文/英文字符串进 widget。
- **图标**：统一用自建 `EtaIcons`（必要时 `EtaMark`），不混用 Material Icons 字形；
  新字形按 `app/eta-tools/eta_icons/` 流程从源集生成登记，不手写字体。
- **注释与文档**：公共 API（service / binding / store 层）给出 dartdoc；涉及协议、坑点
  （如 SQLite 双库并发、`source: process` 预加载）注释写清「为什么」。
- **日志**：不使用 `print` / `debugPrint` 直出敏感信息（cookie / 密钥 / vault 内容）；
  路径、URL 涉及凭据参数时先脱敏。

### 5.2 C 音频引擎（`app/core/audio-engine/`，含 Zig 内核 `kernel/`）

- 管线顺序 `decode → resample → eq → loudness → limiter → tempo → fft → encode(可选)` 不可随意调整；
  新增 DSP 节点需更新架构文档与 CLI 参数表（架构 §5.6）。
- 新解码格式：走 **Zig 内核逐格式渐进接管**路线（`docs/audio-kernel-zig.md`），
  保持「EraAudio 优先尝试、失败回退 FFmpeg/Stable」语义与错误码契约。
- 内存安全：FFI 边界缓冲区大小显式计算并注释（历史段错误案例：FFT 输入缓冲需 `fftSize×32 float`）；
  新增转储 / 探针工具命名遵循 `*_dump` / `*_probe`（已被 .gitignore 覆盖，不入库）。
- Zig 内核第三方来源（参考实现 / 移植表）必须在 `docs/audio-kernel-zig.md` 登记表更新。

### 5.3 C#（`app/core/scanner/`、`app/core/vault/`）

- NativeAOT 约束：避免重型反射 / `dynamic` / 未注解的序列化，发布模式必须可 AOT。
- vault 属安全边界：修改握手协议 / marker / 份额存储逻辑需同步更新 README 安全说明节，
  且测试改动不得把测试后门（`VAULT_TESTING` 条件编译）引入生产构建路径。

### 5.4 C++（`app/core/scraper/`）

- CMake ≥ 3.16；新增依赖优先 header-only 并登记 `THIRD-PARTY-LICENSES.md`；
  多源刮削新增数据源时同步更新 README 数据源列表与设置页文案（i18n）。

### 5.5 Rust（`app/core/downloader/`、`tempo-rs/`、`subsonic/transcoder/`）

- 提交前 `cargo fmt --check` 与 `cargo clippy` 无新增警告；测试放 `tests/`。
- FFI 导出符号（`#[no_mangle] pub extern`）是契约（戒律 10），签名变更需同步 Dart 侧绑定与冒烟测试
  （如 `app/test/downloader_ffi_smoke_test.dart`）。

### 5.6 Go（`app/core/subsonic/`）

- 桌面 FFI 与独立服务端**共享同一份代码**，平台差异用 build tag 区分；新增端点/能力时保证两种形态都可编译、可运行。
- 直写 SQLite 遵守双库访问模型（媒体库只读直读、用户库直读直写，架构 §8），不自行开新连接参数语义。

### 5.7 FFI 资源所有权（跨语言通用）

- **谁分配谁释放**：每个跨边界分配必须有唯一所有权方与唯一释放点；所有权转移必须显式注释
  （`transfer` / `borrow` 语义写进绑定注释），禁止双侧释放或漏释放。
- **缓冲区显式化**：FFI 边界缓冲区大小显式计算并注释依据（历史教训：FFT 输入缓冲须
  `fftSize×32 float`，曾因引擎块 8192 float 越界致段错误）；禁止「先给个大概值」。
- **Dart 侧生命周期**：原生资源必须显式 `dispose` / `stop`，`stop` 路径 join 引擎线程后再清理
  （对齐 `archoera_mediaengine_destroy` 语义）；长生命周期资源可用 Finalizer 兜底，
  但不得依赖它做主清理路径。
- **新增 FFI 符号三件套**：原生侧实现 + Dart 绑定 + 冒烟测试，缺一不合入（§6）。

## 6. 测试与验收门槛

| 层 | 位置 / 命令 | 何时必须跑 |
|---|---|---|
| Dart 静态检查 | `cd app && flutter analyze` | 每次 PR |
| Dart 单元 / Widget | `cd app && flutter test`（`app/test/`） | 每次 PR |
| C 引擎 / C++ / Rust / Go 原生测试 | 各模块 `tests/`；Zig `zig build test`；CI workflow 内置 | 改动对应模块时 |
| vault 端到端 | CI 内置；本地 `app/core/vault/` 测试脚本 | 改动 vault / 会话存储时 |
| 三端构建 | `.github/workflows/build-all.yml`（tag `v*` 或手动触发） | 合入主干后由维护者触发 |
| 手动回归 | 播放 / seek / 切歌 / 登录 / 下载 / 扫描 / 刮削核心路径 | 涉及播放链路与数据面改动时 |
| 基准 / 性能 | `app/core/audio-engine/tests/bench/run_suite.py`（内核单测 + ctest + 逐格式 scorecard + 池并发 + scanner）+ 内存基线对比 | 触及解码 / DSP / 播放 / 内存 / 缓存链路时 |

要求：

- **红牌机制（红线 §2.3）**：PR 未附本地测试证据、或 CI 红仍请求合并 → 直接拒绝。
- 新功能原则上携带测试（解析、状态机、store、门禁类逻辑必须有；纯 UI 布局可用 Widget 冒烟）。
- 修 bug 先补「能复现」的失败测试，再修到绿。
- 测试不得依赖网络真实账号登录态；平台 API 测试用可离线验证的纯函数部分或 mock。
- **基准与性能回归**：触及解码 / DSP / 播放 / 内存链路的 PR，附基准对比（同机同语料，
  附环境与复现命令）或说明为何不适用；内存相关改动附基线对比（对齐架构文档的 memory discipline）。
- **测试素材版权**：测试 / 基准禁用受版权保护的音频；统一使用合成语料（高熵噪声、正弦 / 扫频、
  合成语音）或明确可再分发的许可样本；素材入仓必须附来源与许可说明。

## 7. 提交规范（Commit / PR）

### 7.1 Commit message

遵循 **Conventional Commits**，描述用中文，格式 `type(scope): 摘要`（对齐仓库现有历史）：

| type | 用途 |
|---|---|
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `docs` | 仅文档（`docs/`、README、模块 README、注释性文档） |
| `refactor` | 重构（不改行为） |
| `test` | 仅测试 |
| `chore` | 构建 / 脚本 / CI / 版本号（版本号固定用 `chore(version): x.y.z+n`） |

scope 用模块 / 子系统名，与现有历史保持一致的小写风格，例如：
`playback`、`audio-engine`、`kernel`、`master-pool`、`ui`、`scraper`、`scanner`、`downloader`、
`subsonic`、`vault`、`settings`、`eta`、`test`、`engine`。

示例（取自真实历史）：

```
feat(playback): 内存播放（不落盘）模式 S2/S3 + 文档
fix(audio-engine): segstore 进程池锁改 pthread_once 惰性初始化（MSVC 兼容）
docs(engine): 新增主控+调度池设计（完整 Zig 内核）—— Master/Pool/Registry
chore(version): 0.9.14+5
```

- 摘要一行 ≤ 72 字符为宜；正文可补充动机、方案、验收方式。
- **一个提交一件事**：功能、格式化、文档改动不混提交。
- 提交需可独立构建（至少不破坏 `flutter analyze` / `flutter test`）。

### 7.2 PR 要求

PR 描述需包含：

1. **动机与方案**（关联 Issue 编号，如有）；
2. **改动范围**：列出触及的模块（Dart / C / C# / C++ / Rust / Go / 文档 / CI）；
3. **验收证据**：本地 `flutter analyze` / `flutter test` 全绿结论；原生模块改动附对应构建 / 测试结果；
   **已验证平台**（理想三端全验；未覆盖的端说明原因与风险）；
4. **AI 参与声明**：说明 AI 辅助生成的范围（轻度补全 / 大段生成）；确认已完成人工逐行审核与本地测试
   （红线 §2.3，未满足直接拒绝）；
5. **许可确认**：显式声明接受 §1 贡献者授权（或 GPG/SSH 签名）；
6. **依赖变更**：若引入第三方依赖，附许可兼容性说明与 `THIRD-PARTY-LICENSES.md` 更新（§9）。

维护者按「**红线 → 架构戒律 → 测试门槛 → 文档同步 → 许可合规**」顺序评审：
命中红线无解释直接关闭，不进入后续评审；非红线的不予合并必须给出书面理由（§2.6）。

> 涉及新增数据存储（数据库 / 缓存 / 日志 / 配置 / 临时文件）的 PR，须按 §2.5 在描述中注明存储位置与访问方；
> 涉及平台行为 / 兼容性的 PR，须说明对三端的影响；主张「平台硬性限制」的须附依据（红线 §2.2）。

## 8. 文档规范

- **设计文档**放 `docs/`，命名 kebab-case（如 `engine-master-pool-design.md`）；文首带
  `> 状态：<稿次> · <日期>` 头部；决策变更用「修订块」注明日期与结论，不无痕改写历史结论
  （对齐 `docs/architecture.md` 的写法）。
- **影响架构决策的 PR 必须同步文档**：`docs/architecture.md`（含「附：已确认决策记录」追加条目）、
  受影响的专题文档、README 相应章节。
- **面向用户的功能**（设置项、扫描/刮削行为、快捷操作）同步更新 README 功能描述。
- 模块内约定写模块 `README.md`（如 `app/lib/eta/README.md` 图标体系），不堆进根文档。
- 文档语言：中文为主，代码 / 符号 / 命令保留原文。
- **基准与性能文档**：性能 / 基准结论必须可复现——附环境、语料、方法与复现命令，
  原始数据与评分存 `app/core/audio-engine/tests/bench/`（先例 `SCORE_*.md` / `BENCH_*.md`）；
  对外表述遵守 §2.4：优劣并陈、标注局限。

## 9. 第三方依赖与许可合规

> 详见 [README「许可证」§2.5](README.md#25-第三方代码的约束)，本节为执行清单。

- **许可白名单基线**：MIT / BSD / Apache-2.0 / ISC / MIT-0 / CC0 / OFL，以及 weak-copyleft
  （LGPL-2.1+ / MPL-2.0）在逐项登记后可引入。
- **红线（禁止引入）**：GPL-2.0-only（无 "or later"）、SSPL / BSL / 禁止商业使用的自定义协议、
  非 OSI/FSF 认可的源可用许可。
- **功能红线依赖（§2.1 / §2.2）**：广告 SDK、埋点 / 统计、付费墙 / 内购组件、内网穿透库、
  Node / Python 等非 FFI 运行时绑定——无论其自身许可多宽松，一律禁止引入。
- **供应链与「预制菜」（§2.2）**：高度不建议引入整套第三方实现 / 预编译产物；依赖统一走
  来源可溯 + 许可登记，且**必须公开可获取**（禁止私有 git / 私有 registry 依赖，§2.2）；
  锁定文件（`app/pubspec.lock`、各模块 `Cargo.lock` / `go.sum`）随仓库提交，
  升级依赖须在 PR 说明动机与 diff 审查结论；构建期不得联网拉取未登记产物。
- **登记义务**：每个引入的第三方依赖（直接与间接）必须在**对应模块**的
  `THIRD-PARTY-LICENSES.md` 逐项列明：上游来源、许可证、版权、涉及文件范围。
- **字体 / 图标资源**同样受管：新字形来源与改作说明进 `app/eta-tools/` 对应 README 与根 README 致谢节；
  非 OFL 字体（MiSans / HarmonyOS Sans）不得二次分发源文件或改编。
- **品牌与 fork（品牌未授权）**：AGPL 授予的是**代码**自由，不含品牌——项目名 ArchoeraMusic、logo、
  自有品牌标识字体 EtaMark、官方发布渠道与签名，不得被 fork 用于暗示官方背书。fork 再分发须：
  更换名称与品牌标识、使用自有更新渠道、完整保留 AGPL 合规义务（源码开放 / LICENSE /
  各模块 `THIRD-PARTY-LICENSES.md`）；官方产物仅以本仓库 GitHub Releases 为准，
  非官方渠道分发的产物官方不背书、不提供支持。
- Dart 侧 `pubspec.yaml` 的 `dependency_overrides` 属实验性区（见该文件注释），增删需附验证结论注释。

## 10. 安全规范

- **禁止提交（§2.5 执行项）**：任何真实凭据、cookie、token、密钥文件（`*.key` 已忽略）、个人路径、
  设备名、真实账号测试夹具——代码与测试内一律使用明确标注的假数据；截图先脱敏。
- **数据不出本机（§2.5）**：用户数据仅存本地；新增存储模块在文档与 PR 注明存储位置与访问方；
  任何向第三方的上传（统计 / 崩溃上报 / 云同步等包装）均为红线。
- **定位与权限最小化（§2.5）**：涉及系统定位等敏感权限的能力必须走平台能力外观
  （`docs/platform-capability-facade.md`）：用户显式授权、可关闭、仅限声明用途、无设备指纹与
  追踪标识；权限申请范围与功能声明一致，禁止超范围采集。
- **漏洞私密上报**：**安全漏洞**（触及 vault / 凭据与会话、注入 / 提权 / 数据泄露、供应链投毒）
  一律按 [SECURITY.md](SECURITY.md) 走私密渠道（GitHub 私密漏洞报告），禁止公开 Issue / PR / Discussion
  讨论；修复发布前不公开细节。**正常缺陷**（UI Bug / 调用 bug / 通讯异常 / 崩溃等）仍走公开 Issue（§12）。
- **vault 边界**（README 安全说明节为规范文本）：
  - 测试后门 `ARCHOERA_VAULT_INSECURE_FILE_STORE` 仅存在于 `build-test.sh` 产物，
    绝不允许出现在生产构建路径或发布产物；
  - PROD marker 双重校验（`--version` + 握手 marker）逻辑不得削弱，改动需同步 README 与
    `docs/credential-vault-plan.md`；
  - 新增 env 开关遵循同一信任边界思路：显式 env 只服务测试/CI，生产路径不得设置。
- **凭据落盘**一律走 vault（2-of-2）或 `enc:v1:` 字段级加密，禁止明文写配置 / 数据库 / 日志。
- 涉及网络请求的改动注意：桌面端零 TCP 端口（戒律 1），不出网请求只允许既有平台 API / 刮削源 /
  下载引擎范围内的新端点。

## 11. 仓库卫生

以下内容**禁止入库**（`.gitignore` 已覆盖大部分，新增临时产物先补 ignore 规则）：

- 构建产物：`app/build/`、各模块 `build/`、`target/`、`bin/`、`obj/`、`zig-out/`（按现状）、`dist/`；
- 调试产物：`*_dump`、`*_probe`、`*.pcm`、`*.log`、本地截图（`issuescreenshot/`）；
- 本地参考：`reference/`（内嵌参考仓库）、`Icon化/`；
- 大文件：`*.tar.gz`、`*.apk`；确需入库的二进制资产（字体等）先在 PR 说明理由与许可。

分支约定：

- `main` 为主干，保持可构建可测试；
- 功能开发用特性分支（`feat/xxx`、`fix/xxx`），PR 合入，不直接推主干（维护者小修可直接推但需符合 commit 规范）。
- **历史保护**：`main` 禁止 force push 与已推送历史的改写 / rebase；纠错走新提交（优先 `revert`）。
- **签名要求**：维护者的敏感提交——LICENSE 变更、版本发布（`chore(version)`）、本规范与 CI 变更——
  必须 GPG / SSH 签名（许可升级的签名要求见 [README §2.4](README.md#24-升级流程预先约定)）。

## 12. Issue 与缺陷报告

> **边界**：正常缺陷走公开 Issue——可见 UI Bug、未触及安全模块的调用 bug、模块间通讯异常、
> 非正常程序崩溃等，照常使用下方格式与 Issue 模板；
> **安全漏洞不走此处**——触及 vault / 凭据与会话、注入 / 提权 / 数据泄露、供应链投毒等，
> 一律按 [SECURITY.md](SECURITY.md) 走私密渠道，公开 Issue 会放大风险。

缺陷报告请包含：

1. **平台与版本**：Linux / Windows / macOS + 应用版本（设置 → 关于，或 bundle 内 `--version`）；
2. **引擎模式**：Stable(FFmpeg) / EraAudio；内存播放 / 文件模式（若涉及播放）；
3. **复现步骤**（最小化）与**期望 / 实际行为**；
4. **日志**：`logs/` 对应片段——**先脱敏**（去除 cookie / token / vault 报文）；
5. 若为音质/解码问题：附样本格式信息（容器 / 编码 / 采样率 / 声道），必要时提供可复现的最小样本链接。

功能请求请说明使用场景与三端范围预期；架构级提案请先读 `docs/architecture.md` 决策记录，避免重复已否决方案（如侧车、libmpv 直连）。

> 社区行为约束（§2.4）同样适用于 Issue / PR 讨论：只谈事实、数据与复现步骤，不评价人与其他项目；
> 引用 EraAudio 相关基准时附上口径与局限声明，防止断章取义。

## 13. 维护者职责（版本 / 发布 / 决策记录）

- **版本号**：`app/pubspec.yaml` `version`，语义化 `x.y.z[-预发布][+构建号]`（语法约束见该文件注释）；
  发布流程 `chore(version): x.y.z+n` → 打 `v*` tag 触发 CI 三端构建与 Release。
- **发布前检查**：`flutter analyze` / `flutter test` 全绿；三端 CI 构建（workflow_dispatch 手动触发一次）
  通过；README 版本相关基准 / 功能描述仍准确；`THIRD-PARTY-LICENSES.md` 与实际依赖一致。
- **决策记录**：架构决策变更（新增戒律 / 推翻既有决策）需：更新 `docs/architecture.md` 决策记录
  追加编号条目 + 修订相关专题文档 + 在 PR 中显著标注，必要时开公告 Issue 收集反馈。
- **评审纪律（§2.6）**：禁止借权限威胁、禁止设隐性合并门槛、禁止无故搁置合格 PR；
  拒绝合并须给出书面理由（红线命中除外，按无解释直接关闭执行）。
- **许可升级**：仅按 [README §2.4 流程](README.md#24-升级流程预先约定) 执行（公告期 ≥ 30 天 + 签名提交）。
