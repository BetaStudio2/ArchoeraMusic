# ArchoeraOS：initrd 阶段强制加载 dm-crypt。
#
# 加密安装（LUKS2 root）时，systemd-cryptsetup 需要 dm-crypt；虽然 dm-crypt.ko 已经在
# initrd 里（kmod 元数据也在），但加密设备没有 modalias 事件可触发自动加载，
# 所以显式列进 modules-load.d 兜底，避免「解锁时找不到 dm_crypt」。
dm-crypt
cryptd
