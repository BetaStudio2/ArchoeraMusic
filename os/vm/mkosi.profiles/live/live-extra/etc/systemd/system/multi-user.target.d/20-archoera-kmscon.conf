# ArchoeraOS Live：在 tty3 拉起 kmscon（可显示中文的控制台）。
#
# 内核自带 console 的字形上限放不下 CJK，装中文字体也没用；kmscon 走 DRM/KMS +
# Pango/fontconfig（配合基础镜像里的 noto-fonts-cjk）才能显示中文。
# 位置选 tty3：tty1 是 kiosk、tty2 是安装器的 root 自动登录，三者互不抢占。
[Unit]
Wants=kmsconvt@tty3.service
