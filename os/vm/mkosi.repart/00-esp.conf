# ArchoeraOS 自定义 ESP：mkosi 默认按内容/512M 定死，而内核 + 主 initrd + 内核模块 initrd
# + 微码 initrd 会超过 512M（写 ESP 时 mcopy 报 Disk full）。这里固定给 1G。
[Partition]
Type=esp
Format=vfat
CopyFiles=/boot:/
CopyFiles=/efi:/
SizeMinBytes=1G
SizeMaxBytes=1G
