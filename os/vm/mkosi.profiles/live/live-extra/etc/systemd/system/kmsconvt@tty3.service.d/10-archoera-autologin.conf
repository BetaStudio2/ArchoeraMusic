# ArchoeraOS Live：tty3 直接给一个 root 登录 shell（与 tty2 的 root 自动登录一致），
# 否则 kmscon 默认跑 /bin/login，而 Live 里 root 密码是锁的 → 等于进不去。
[Service]
ExecStart=
ExecStart=/usr/bin/kmscon --vt=%I --no-switchvt --login -- /bin/bash -l
