# Archoera-Music 项目健壮性百分制评分报告

> **项目版本**：v0.8.7-pre.1+rev.1  
> **分析日期**：2026-08-12  
> **综合得分**：**61 / 100**

---

## 一、总览评分表

| 维度 | 得分 | 等级 | 说明 |
|------|------|------|------|
| **架构设计** | **80** | 良好 | FFI 直连架构清晰，模块解耦程度高 |
| **性能优化** | **80** | 良好 | 自定义帧节流、PCM 按需计算、节能模式优秀 |
| **资源管理** | **80** | 良好 | FFI 生命周期严谨，并发安全，断点续传 |
| **代码质量** | **65** | 中等 | 空安全到位，但复杂度偏高、字符串枚举多 |
| **运维可靠** | **60** | 及格 | 三平台 CI 完善，但测试弱、日志缺 |
| **安全防护** | **55** | 不及格 | SQL 注入防护好，但明文存凭据、授权松 |
| **错误处理** | **45** | 不及格 | 有错误类型体系，但全局边界缺、静默吞噬严重 |
| **测试覆盖** | **30** | 很差 | Dart 业务层 <10%，Go/C#/C++ 近乎为零 |

**综合得分：61 / 100（中等偏下）**

---

## 二、各维度详细评分

### 1. 架构设计：80 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| 模块解耦 | 85 | FFI 直连替代 IPC，接口边界清晰，5 大引擎独立 |
| 可替换性 | 75 | 音频引擎、流媒体后端、下载引擎均可替换 |
| 分层合理性 | 80 | UI/服务/原生三层清晰，Riverpod 状态管理规范 |
| 平台抽象 | 80 | 平台 API 纯 Dart 化，流媒体多协议统一抽象 |
| 依赖方向 | 80 | 单向依赖，无循环依赖 |

**核心优势**：FFI 直连架构避免了 IPC 开销和安全隐患；每个原生模块通过 C ABI 导出有限符号；平台 API 与原生模块无耦合。

---

### 2. 性能优化：80 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| 节能帧节流 | 95 | 自定义 PowerSavingFrameBinding，三级降帧（60→5→1 FPS） |
| 内存管理 | 75 | LRU 缓存、PCM 按需计算、分页读取，但缺磁盘图片缓存 |
| 列表虚拟化 | 70 | ListView.builder 懒构建，但数据层全量在内存 |
| FFT 降帧 | 80 | 位置事件驱动 + 100ms 节流，节能模式 300ms |
| 图片优化 | 70 | cacheWidth/cacheHeight 限制解码尺寸，但缺磁盘缓存 |
| 并发调度 | 90 | 阻塞操作全走 Isolate，代际号防竞态 |

**核心亮点**：自定义帧节流节能模式是项目一大亮点，事件驱动而非轮询，多场景分级降帧。

---

### 3. 资源管理：80 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| FFI 生命周期 | 90 | 各语言均有明确 destroy 流程，"戒律"级约束 |
| 内存释放 | 85 | 回调内存立即释放，C 侧完整 free 链 |
| 文件句柄 | 75 | 大部分有 dispose/close，但 SQLite 单例无关闭时机 |
| 流与订阅 | 80 | StreamController/Timer/Subscription 均正确关闭 |
| 数据库连接 | 70 | busy_timeout 设置了，但连接管理较简单 |
| 崩溃恢复 | 80 | 播放会话快照、下载断点续传、历史数据迁移 |

**核心亮点**：FFI 资源管理非常严谨，甚至有「戒律 13.3」这样的硬约束文档化。

---

### 4. 代码质量：65 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| 空安全 | 85 | Dart 空安全全面启用，fromJson 防御性编程 |
| 类型安全 | 60 | 模型类良好，但 source/quality/repeatMode 为字符串 |
| 代码复用 | 55 | 三大平台 API 重复度高，缺公共基类 |
| 代码复杂度 | 55 | PlaybackNotifier 单文件 1274 行，圈复杂度高 |
| 代码规范 | 75 | analysis_options 配置，CI 执行 dart analyze |
| 注释文档 | 70 | 代码注释丰富，"戒律"说明清晰，但缺 API 文档 |

**主要问题**：PlaybackNotifier 职责过多；三大平台 API 结构高度相似，重复代码多。

---

### 5. 运维可靠：60 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| CI 三平台覆盖 | 80 | Linux/Windows/macOS 均有自动化构建 |
| CI 测试执行 | 20 | 仅跑 1 个文件 2 个用例，PR 不触发 |
| 打包发布 | 70 | Linux 6 种格式，但缺自动发布和签名 |
| 日志系统 | 25 | 各语言各自为政，无持久化，无级别控制 |
| 监控告警 | 15 | 仅开发者模式 FPS 监控，无错误上报 |
| 依赖版本锁定 | 75 | pubspec.lock/Cargo.lock/go.sum 已锁定，系统依赖浮动 |
| 文档完整性 | 75 | 架构文档详细，缺 API 文档和贡献指南 |

**主要问题**：日志系统严重不足；CI 测试覆盖极低；PR 提交不触发构建。

---

### 6. 安全防护：55 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| SQL 注入防护 | 85 | Go Subsonic 全面参数化查询 |
| XSS 防护 | 70 | Flutter 原生渲染风险低，Subsonic XML 有转义 |
| CSRF 防护 | 35 | 无 CSRF 机制（但桌面端场景风险较低） |
| 敏感信息存储 | 30 | streaming_servers.json 和 netease_session.json 明文存储 |
| 认证机制 | 45 | 符合 Subsonic 规范，但支持明文密码、无哈希 |
| 授权机制 | 40 | 基础角色存在，但 GetUsers 等端点无权限检查 |
| 加密算法 | 65 | 自研用标准算法，但部分为第三方 API 要求的弱模式 |
| 密钥管理 | 65 | 密钥文件权限正确，但与数据同机 |
| 依赖安全 | 40 | 无自动化漏洞扫描流程 |
| 安全销毁 | 85 | 3 遍覆盖 + 主动失效 + 二次确认，设计完善 |

**最高风险**：敏感凭据明文存储（P0 级问题）。

---

### 7. 错误处理：45 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| 错误类型体系 | 65 | 各模块有专门异常类型，流媒体有继承体系 |
| 全局错误边界 | 10 | 完全缺失，无 FlutterError.onError / runZonedGuarded |
| 异步错误处理 | 50 | try-catch 较普遍，但静默吞噬多、堆栈捕获少 |
| 错误静默吞噬 | 20 | 40+ 处 catch (_) {}，仅 3 处捕获完整堆栈 |
| 输入验证 | 55 | JSON 解析防御良好，但用户输入验证不足 |
| 降级策略 | 80 | 多源换源、连续失败保护、音质降级、节能降级 |
| 错误上报 | 5 | 无任何错误上报机制 |

**最严重问题**：全局错误边界缺失 + 大量静默错误吞噬。

---

### 8. 测试覆盖：30 / 100

| 子项 | 得分 | 说明 |
|------|------|------|
| Dart 单元测试 | 20 | 仅 EventBus + PlaybackSession 有单元测试 |
| Dart 集成测试 | 35 | 8 个集成测试文件，但 CI 不执行 |
| Widget 测试 | 5 | 几乎为零 |
| Rust 测试 | 50 | 加密算法有字节级对拍测试 |
| C 单元测试 | 45 | DSP 模块（FFT/EQ/limiter/loudness）有基础测试 |
| Go 测试 | 0 | 完全无测试 |
| C# 测试 | 0 | 完全无测试 |
| C++ 测试 | 0 | 完全无测试 |
| CI 测试集成 | 10 | 仅跑 1 个文件 |
| 覆盖率工具 | 5 | 未配置任何覆盖率统计工具 |

**最薄弱环节**：Go Subsonic / C# Scanner / C++ Scraper 零测试。

---

## 三、等级评定标准

| 分数区间 | 等级 | 描述 |
|----------|------|------|
| 90-100 | 优秀 | 业界领先水平，健壮性极高 |
| 80-89 | 良好 | 设计合理，风险可控 |
| 70-79 | 中等偏上 | 整体不错，有少量改进空间 |
| 60-69 | 及格 | 基本可用，存在明显短板 |
| 50-59 | 不及格 | 问题较多，需重点改进 |
| 40-49 | 较差 | 严重不足，影响生产使用 |
| < 40 | 很差 | 极度薄弱，存在重大风险 |

---

## 四、关键发现总结

### ✅ 强项（≥80 分）
1. **架构设计（80）**：FFI 直连架构精良，模块解耦彻底
2. **性能优化（80）**：节能帧节流模式是行业领先水平
3. **资源管理（80）**：FFI 生命周期管理严谨，并发安全

### ⚠️ 中等项（60-79 分）
4. **代码质量（65）**：空安全到位，但复杂度和重复度待优化
5. **运维可靠（60）**：CI 完善但测试薄弱，日志系统缺失

### ❌ 弱项（<60 分）
6. **安全防护（55）**：明文存凭据是最大风险点
7. **错误处理（45）**：全局边界缺失 + 大量静默吞噬
8. **测试覆盖（30）**：多语言生态测试严重不足

---

## 五、改进路线图（按投入产出比排序）

### 第一阶段：快速收益（1-2 周）
| 改进项 | 预期提升 | 难度 |
|--------|----------|------|
| 添加全局错误边界 | 错误处理 +15 | 低 |
| 减少静默错误吞噬（加日志） | 错误处理 +10 | 低 |
| PR 触发 CI 构建 | 运维可靠 +10 | 低 |
| 原子化 JSON 写入 | 资源管理 +5 | 低 |

### 第二阶段：重点突破（1-2 月）
| 改进项 | 预期提升 | 难度 |
|--------|----------|------|
| 加密存储敏感凭据 | 安全防护 +20 | 中 |
| 统一日志系统（持久化） | 运维可靠 +20 | 中 |
| Go Subsonic 单元测试 | 测试覆盖 +10 | 中 |
| HTTP 指数退避重试 | 错误处理 +10 | 中 |

### 第三阶段：长期优化（3-6 月）
| 改进项 | 预期提升 | 难度 |
|--------|----------|------|
| Dart 核心业务单元测试 | 测试覆盖 +15 | 高 |
| 拆分 PlaybackNotifier | 代码质量 +10 | 中 |
| 依赖安全扫描集成 | 安全防护 +10 | 中 |
| 完善 Windows 支持 | 运维可靠 +10 | 高 |
| 提取平台 API 公共基类 | 代码质量 +10 | 中 |

---

*报告生成时间：2026-08-12 | 基于 Archoera-Music v0.8.7-pre.1 源码分析*
