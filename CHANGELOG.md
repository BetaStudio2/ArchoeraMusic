# Changelog

本文件是各版本 GitHub Release 说明的来源：发版时 release 作业按 tag 抽取对应
`## [<version>]` 段落作为 Release 正文（见 `.github/workflows/build-all.yml`）。
版本号即 git tag（去掉 `v` 前缀），日期为该版本发布日（UTC）。

## [0.9.16+3] - 2026-09-12

你好喵～
上一版刚搬完家，结果水管漏了，这次是通下水道（累喵）：

1、修好了 Linux **一启动就吃满内存卡死**的问题：窗口状态桥接在枚举顶层窗口时陷入死循环，改用官方 `gtk_application_get_windows`，并把 GTK 调用延后到主循环空闲执行
2、修好了**播放时应用「未响应」**：主线程上还有几处同步 DBus 调用（推送媒体信息时的 flush、注册防休眠时的 Inhibit、懒建连），全部改成非阻塞 / 丢到后台线程
3、修好了**从其它全屏应用切回来会卡死**：之前切回时会同步去读系统强调色，现在不再在主线程上等它
4、系统强调色与深浅色统一改成**平台推送**（让应用「被知道」，而不是主动去问），三端一致；顺带把**主题模式持久化**了，重启不再偷偷变回深色
5、启动页重做：字标换成 Manrope（低调、不浮夸），底部小字挪到窗口最底下
6、清理了一批代码注释与绝对路径（改成相对路径）
7、新增本地交叉验证脚本 `app/tool/verify_macos.sh` 与 `app/tool/verify_windows.sh`：改完原生桥接，不用真机也能先验一遍能不能编过

嗯，通完下水道感觉整个人都通透了……

## [0.9.16+2] - 2026-09-12

你好喵～
这次是后端大搬家 + 一堆收尾（搬家真的累喵）：

1、平台能力桥接**全面从 Zig 迁移到各平台原生实现**：Windows 用 MSVC C++/WinRT、Linux 用 libdbus、macOS 用 ObjC++，构建改用 CMake；接口（`apl_*`）没变，Dart 侧零改动
2、Windows 的媒体面板 / 蓝牙耳机按键改用 `MediaPlayer` 方案，真机验证**正常**了（之前的崩溃和死锁都埋掉啦）
3、Linux 打包补上 `dbus` 依赖，Arch 包可以直接装
4、修了封面配色：偏白 / 近中性的封面不再被系统调色盘放大成奇怪的颜色，纯灰封面也能保留本色
5、macOS 的媒体键暂时还没真机验证（我没 Mac 嘛 qwq），开了个 issue 长期跟踪（#1），有 Mac 的朋友欢迎帮忙喵
6、系统强调色改用各平台官方接口（Linux 走 XDG Desktop Portal、Windows 走 `UISettings`、macOS 走 `controlAccentColor`），跨桌面环境更稳啦
7、修好了系统通知：**安装版**会在开始菜单建带 AppUserModelID 的快捷方式（Windows Toast 的前提，以前根本发不出来），macOS 换用新的 `UNUserNotificationCenter`（旧的 `NSUserNotification` 已废弃）；**便携版不建任何快捷方式**，保持无痕

嗯……搬完家让我躺一会……

## [0.9.15+8] - 2026-09-12

你好喵～
本次主要修复 Windows 平台能力（真的，别骂了qwq）：

1、媒体面板/蓝牙耳机按键现在能真正操作软件了
2、修好了本地音乐封面导致崩溃的问题
3、修好了 Windows 构建偶发的内核链接失败
4、macOS 的 Toast 与媒体键注册也一并修正

饿了，今天吃点什么呢……

## [0.9.15+7] - 2026-09-12

你好喵
我们本次带来了大量更新

1、来，吃饼（
2、增强了桥接模块的能力
3、正在逐渐支持蓝牙等的控件能力
4、增加了三种播放页效果
5、EraAudio下的EraSync目前已经可正常运行，纯内存解码能力增强awa
6、增强了扫描器的异步能力
7、可以设置快捷键了
8、完善了高级歌词的逻辑，现在不会那么容易“飞起来”了awa
9、引入标准GPU渲染器，降低了99.99%的卡顿，超越了全球99.99%的玩家！（中二犯了）
10、修复了一堆历史遗留问题，现在可以愉快的播放Assumptions了awa！（bushi）
11、对了，播放引擎现在会自动换行了
12、我喜欢你！

好了，以上就是本期节目的全部内容了，如果你喜欢我们的节目的话，还劳烦点一个Star喵，你的认可就是我们前进最大的动力喵！

## [0.9.15+4] - 2026-09-10

泥嚎喵~

本次进行了较大的更新

1、EraSync正式加入，现在EraAudio可以更好的承载更大的负载了
2、完善了各平台的系统原生桥接能力，现在的接入与稳定性会比以前更好了
3、媒体库扫描器已接入EraSync
4、修复了UI不能被UI的bug
5、强化了实例单开，现在不会再出现2、200、2000、20000个自己了awa
6、你真好看……欸嘿嘿……

饼！饼！饼！

## [0.9.14+5] - 2026-09-08

你好喵

以下是本次更新塞进的石山（？）：

1、修复了Windows平台下的若干bug（问就是记不清了，msvc依旧大力出奇迹，哦对，是Unicode的越界问题，神知道边缘处理不够劲啊qwq……）
2、新增了内存化播放的功能，现在可以让播放器的音频流完全只通过运行内存完成操作了awa，然后修改了workflow的逻辑，让其能够适应新的模块
3、内存化虽好，不要贪杯哦～超量的设置会让你的电脑飞起来！
4、重写了媒体扫描器的逻辑，现在的扫描性能应该会有所增强）
5、你知道吗：你不知道
6、没了
7、对，还优化了一点界面显示（优化在哪？我看并非）

然后rua了一会毛绒UwU……

画饼……画饼……画饼……唉嘿嘿……>_<

## [0.9.14+2] - 2026-09-07

书接上回

那更新的内容不可预知能力很好了

本次带来了以下更新喵：
1、使用了更好的图标，现在的图标看起来不会那么古老了
2、对扫描器部分模块进行重构，改为扫描后合并，降低单线程写入器对整体性能的影响
3、优化部分UI程序段，增加Mixin能力，降低代码量与相对维护难度
4、更新局部UI，让其看起来更像人（

（画饼）后期会考虑更新更多播放页背景喵

谢谢你对ArchoeraMusic的支持喵

## [0.9.13+0] - 2026-09-07

你好喵
本次的重磅更新内容为：

1、修复了一些未知问题（嗯？）

awa

## [0.9.12+11] - 2026-09-06

因为发现可以改更新日志更新日志，所以结合标准的“更新规范”，就写点更新日志吧

1、增强对QM的基础搜索能力
2、修复了一些已知问题
3、增强Flutter的UI布局
4、看了眼歌词表现
5、同以上四条


然后抽卡歪了（

## [0.9.12+3] - 2026-09-06

awa+3

版本排序有点乱喵
别下错了喵
谢谢喵

## [0.9.12+0] - 2026-09-06

awa+2

## [0.9.11+5] - 2026-09-05

awa+1
