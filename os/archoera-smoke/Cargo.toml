[package]
name = "archoera-smoke"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "ArchoeraOS 冒烟客户端：用最小 xdg-toplevel + wl_shm 动画验证合成器渲染通路"

[dependencies]
# 客户端工具集：compositor / xdg-shell / shm / registry 绑定。
smithay-client-toolkit = { version = "0.19", default-features = false, features = ["calloop"] }
wayland-client = "0.31"
anyhow = "1"
