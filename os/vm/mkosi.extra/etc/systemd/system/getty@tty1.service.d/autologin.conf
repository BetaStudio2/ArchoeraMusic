# ArchoeraOS：tty1 以默认用户 archoera 自动登录并直接进入 kiosk 会话
# （不再用 root；mkosi 的 Autologin=yes 已关闭，避免与本文件冲突）。
[Service]
ExecStart=
ExecStart=-/sbin/agetty -o '-p -f -- \\u' --noclear --autologin archoera %I $TERM
