# Live 专属：tty2 以 root 自动登录，作为「安装器入口 / 现场排查」控制台。
# 真机上按 Ctrl+Alt+F2 切到 tty2，即可执行：
#   archoera-install /dev/nvme0n1     （目标磁盘务必确认）
# 已安装系统不带本 drop-in（live-extra 只在 live 镜像里）。
[Service]
ExecStart=
ExecStart=-/sbin/agetty -o '-p -f -- \\u' --noclear --autologin root %I $TERM
